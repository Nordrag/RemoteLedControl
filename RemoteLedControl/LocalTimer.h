#pragma once
#include <vector>

class Timer {
public:
    double from;
    double to;

    // Add any other properties or methods you need
};

class Day {
public:
    int day;
    std::vector<Timer> timers;
    // Add any other properties or methods you need
};
